# Journey-versus-gate inventory

<!-- Generated by `node scripts/journey-gate-inventory-gate.mjs --write` from src/lib/journey-gate-inventory.js. Do not edit by hand: the gate fails while this file is stale. -->

**37 of 57 user journeys are proven (65%)** — a browser or deployed gate exercises the journey itself. 16 more have their helpers tested and the journey never exercised. 4 have nothing. 13 are proven against a live deployment. 2 more are parked: hidden behind a build flag, unreachable in this build, counted apart.

Floors that may only rise: proven ≥ 37, on the deployment ≥ 13. The gate fails on a drop and on a stale floor, so these are the counts.

## What a level proves

| level | what runs | what it proves |
|---|---|---|
| deterministic | a unit or contract test, or a gate with no browser and no model | the helpers behind a journey — not the journey; the attachments path had passing helpers on the day it dropped four files |
| browser | headless Chromium through the real rendered UI, upstreams stubbed | the journey, on this build |
| deployed | the same against a live deployment; a `golden:` name is one of the deployed golden transactions, a real model turn | the journey, in production |

## Unproven journeys — the list to work down

### Nothing (4)

- **Export my data, or delete my account** (`account-export-delete`, front door) — No test opens the account handler. A deletion that fails silently, or lands on the wrong account, would be found by a user.
- **Inline autocomplete while typing a prompt** (`prompt-autocomplete`, chat) — The endpoint is proven to refuse a blocked session and to boot (platform rows). Nothing proves a completion ever arrives in the composer.
- **Switch the Preview device, download the build, or ask to Improve it** (`preview-controls`, coding desk) — Four durable hooks and no gate reads any of them.
- **Connect a custom domain** (`connect-domain`, coding desk) — The domains function is proven to boot and to refuse a blocked session (platform rows). Nothing proves a domain connects.

### Helpers only (16)

- **Watch the guest build demo before signing in** (`guest-build-demo`, front door) — The contract test pins that the hero CTA opens auth rather than a guest animation; the demo itself has never been rendered by a gate.
- **Sign in with Google** (`sign-in-google`, front door) — Provider configuration and account linking are tested; the button, the redirect and the session that follows are not.
- **Sign in with GitHub** (`sign-in-github`, front door) — Same gap as Google: the OAuth round trip has never been driven.
- **Reset a forgotten password** (`password-reset`, front door) — The public copy and the hash are tested; the request-and-confirm flow is not.
- **Polish the prompt with the wand** (`prompt-enhance`, chat)
- **Switch Studio between plan and build** (`studio-mode-plan-build`, chat) — The mode logic is tested; the toggle has never been clicked by a gate.
- **Rewind the desk to an earlier checkpoint** (`desk-rewind`, coding desk) — Checkpoints are tested; the menu has never been opened by a gate. Durable rewind (Phase 7, 2026-09-07): desk-checkpoints.js said in its own header that cross-reload history needed a server-side home, and the obstacle it named was size -- twenty full copies of a working tree per session is not a database row. Each stored checkpoint is now the delta since the one before it, with the tree hash recorded alongside, and the replay verifies that hash after EVERY step. A delta chain has one failure a pile of snapshots does not: a damaged or missing link yields a tree assembled from two moments, which looks fine and is not, so it is refused and located instead -- the caller is told which checkpoint stopped being trustworthy and is handed the last state that verified. A hole in the stored sequence is caught before replay, because applying the wrong delta to the wrong tree would blame an intact checkpoint for the mismatch. A checkpoint too large to store is refused with its size rather than truncated: a partial checkpoint is what someone rewinds TO. Review of #591 found three more: a save REPLACES a session's chain rather than adding to it, because the desk trims its own history and renumbers what remains, so checkpoint 21 would arrive claiming the position the dropped one still held and every later read would refuse the chain forever -- each save writes a fresh generation and readers take the newest, so a chain is never seen half-replaced; the rows carry the person's source so they cascade on account deletion and appear in the account export, which had still queried only the five tables that predated them; and the desk now reads the stored chain back on session open, rebuilding and re-verifying it locally rather than trusting the answer, and adopting it only when the session has not already moved on.
- **Deploy a full-stack app to Cloud Run** (`deploy-gcp`, coding desk) — Proven to be served; never driven.
- **Connect GitHub and choose the repository and branch a build writes to** (`github-connect-destination`, coding desk) — Five durable hooks; no browser gate has connected GitHub or opened the destination menu.
- **Import an existing repository into the desk** (`github-import-repo`, coding desk)
- **Open, review, comment on and merge pull requests from the desk** (`github-pull-requests`, coding desk)
- **Research: a brief, claims verified, a topic watched, a deep dive** (`research-desk`, advisor desks) — The workspace contract opens the desk. No gate has run a verify, a watch or a deep dive through the board.
- **Bring your own provider keys for this session** (`privacy-vault-byok`, other surfaces) — The server reads the keys correctly. Nothing proves the desk sends them, or that a turn runs on them.
- **Send product feedback** (`feedback-widget`, other surfaces)
- **Admin: metrics, analytics, feedback triage and the model dashboard** (`admin-dashboard`, other surfaces) — Ranking and lifecycle are tested; the metrics and feedback handlers, and every admin screen, are not.
- **Open the desk at its own address after signing in** (`isolated-desk-route`, other surfaces)

### Parked (2) — unreachable in this build, not counted above

- **Dream-to-Action canvas: an idea becomes a thought, then an action** (`dream-to-action`, other surfaces) — Parked on 2026-09-06 to keep the surface inside what the gates can prove: the tab, the landing card and the desk's "push to canvas" control render only with VITE_QUANTORA_EXPLORATORY_SURFACES=on. No durable hook, no test, no gate.
- **Quantum Playground: build a circuit and watch the state** (`quantum-playground`, other surfaces) — Parked on 2026-09-06 to keep the surface inside what the gates can prove: the tab and the landing card render only with VITE_QUANTORA_EXPLORATORY_SURFACES=on. The simulator is tested; the playground has no durable hook and no gate.

## User journeys

### front door

| journey | what a user does | model turn | deterministic | browser | deployed | status |
|---|---|---|---|---|---|---|
| `landing-enter-studio` | Land on the marketing page and enter Studio | no | `deployed-gate-contract.test.js` | `workspace-contract-browser-gate.mjs` | `golden:calculator` | proven |
| `guest-build-demo` | Watch the guest build demo before signing in | no | `deployed-gate-contract.test.js` | — | — | helpers only |
| `sign-in-email-password` | Sign up or sign in with email and password | no | `password.test.ts`, `auth-response.test.ts`, `auth-privacy.test.ts`, `session-cookie.test.ts`, `auth-modal-styles.test.js` | `front-door-browser-gate.mjs` | — | proven |
| `sign-in-google` | Sign in with Google | no | `auth-env.test.ts`, `auth-response.test.ts` | — | — | helpers only |
| `sign-in-github` | Sign in with GitHub | no | `auth-env.test.ts`, `auth-response.test.ts`, `github-principal.test.ts` | — | — | helpers only |
| `password-reset` | Reset a forgotten password | no | `auth-privacy.test.ts`, `password.test.ts` | — | — | helpers only |
| `session-restore-sign-out` | Come back still signed in, and sign out | no | `session-cookie.test.ts`, `session-bearer.test.ts`, `session-context.test.ts`, `useStudioSession.test.js`, `studio-session-storage.test.js` | `front-door-browser-gate.mjs` | — | proven |
| `desktop-sign-in-handoff` | Sign in to the desktop app through the browser handoff | no | `desktop-auth.test.ts`, `desktop-auth-handoff.test.js`, `desktop-bridge-contract.test.js`, `auth-flow.test.ts` | `desktop-smoke-gate.mjs` | — | proven |
| `account-export-delete` | Export my data, or delete my account | no | — | — | — | nothing |
| `profile-picture` | Set a profile picture or pick an avatar | no | — | `media-profile-browser-gate.mjs`, `workspace-contract-browser-gate.mjs` | — | proven |

### chat

| journey | what a user does | model turn | deterministic | browser | deployed | status |
|---|---|---|---|---|---|---|
| `chat-turn` | Ask Quantora something and read the reply | yes | `turn-never-silent.test.js`, `turn-failure-sentence.test.js`, `conversation-policy.test.ts`, `model-quality-outcome.test.ts`, `outcome-navigator-eval.ts`, `studio-synthetic-test.mjs` | `workspace-contract-browser-gate.mjs`, `code-highlight-browser-gate.mjs` | `golden:guided-intake` | proven |
| `chat-attachments` | Attach documents or images to a turn, and see what was read | yes | `attachment-text.test.ts`, `chat-attachments.test.js`, `research-pdf-text.test.ts` | `attachments-browser-gate.mjs` | `golden:document-grounded`, `golden:brief-with-documents` | proven |
| `failed-turn-honesty` | A turn that fails says so, heals once, and never ends blank | yes | `turn-heal-contract.test.js`, `turn-recovery.test.js`, `coding-outcome-spine.test.js`, `turn-escalation.test.js`, `desk-edit-proof.test.js` | `self-heal-browser-gate.mjs`, `qir-production-recovery-browser-gate.mjs`, `desk-edit-honesty-browser-gate.mjs` | `golden:guided-intake`, `golden:iterate-heading` | proven |
| `decision-modal` | Answer the desk when it asks a clarifying question | yes | `intake-modal-contract.test.ts`, `assistant-modal.test.js`, `desk-chat-claim-filter.test.js` | `guided-intake-browser-gate.mjs` | `golden:guided-intake` | proven |
| `plus-tools-menu` | Open the plus menu and pick a tool | yes | `studio-tools-menu.test.js` | `study-media-browser-gate.mjs` | — | proven |
| `prompt-enhance` | Polish the prompt with the wand | yes | `prompt-enhancement.test.ts`, `prompt-polish-guard.test.js`, `prompt-polish-failure.test.js` | — | — | helpers only |
| `prompt-autocomplete` | Inline autocomplete while typing a prompt | yes | — | — | — | nothing |
| `model-pick-dual-arena` | Pick a model, or compare two in the dual arena | yes | `model-routing.test.js`, `model-catalog.test.js`, `no-hardcoded-model-ids.test.js` | `workspace-contract-browser-gate.mjs`, `studio-regression-browser-gate.mjs`, `travel-browser-release-gate.mjs` | — | proven |
| `studio-mode-plan-build` | Switch Studio between plan and build | no | `studio-mode.test.js`, `build-intent.test.js` | — | — | helpers only |
| `turn-lane-planned` | Send a brief and have the platform decide what to make from its meaning, not from a word | yes | `turn-planner.test.ts`, `turn-plan-client.test.js`, `office-intent.test.js`, `turn-plan-ledger.test.ts` | `turn-planner-browser-gate.mjs` | `golden:brief-with-documents` | proven |
| `chat-sessions-manage` | Start a new chat; rename, search, archive, or file one under a project | no | `project-store.test.js`, `session-desks.test.js`, `session-activity.test.js` | `desk-job-review-browser-gate.mjs`, `second-transaction-browser-gate.mjs` | `golden:simple-website` | proven |
| `workspace-owns-its-chats` | Open a chat inside a workspace from its "+", fold its chats, and stay on that desk | no | `useStudioSession.test.js`, `studio-domain-inference.test.ts`, `request-normalizer.test.ts` | `coding-desk-sticky-browser-gate.mjs` | — | proven |
| `long-session-continues` | Keep a long session going: old turns fold into a digest, and one click continues in a new chat that carries the goal, facts and desk | no | `history-budget.test.js`, `session-continuity.test.js`, `useStudioSession.test.js` | `session-handover-browser-gate.mjs` | — | proven |
| `fork-resume-session` | Fork a message, or resume where a session left off | no | `session-continuity.test.js` | `studio-regression-browser-gate.mjs`, `study-media-browser-gate.mjs`, `travel-browser-release-gate.mjs` | — | proven |

### coding desk

| journey | what a user does | model turn | deterministic | browser | deployed | status |
|---|---|---|---|---|---|---|
| `build-from-prompt` | Build a website or app from a prompt | yes | `pipeline-stress.mjs`, `build-intent.test.js`, `build-intent-scope.test.js`, `build-session.test.js`, `coding-turn-planner.test.js`, `candidate-patch.test.js`, `outcome-state.test.js` | `coding-desk-must-write-files-browser-gate.mjs`, `second-transaction-browser-gate.mjs`, `studio-regression-browser-gate.mjs`, `coding-desk-sticky-browser-gate.mjs`, `desk-edit-honesty-browser-gate.mjs` | `golden:calculator`, `golden:simple-website`, `golden:business-tool`, `golden:brief-with-documents`, `golden:iterate-heading` | proven |
| `preview-built-site` | See the build run in Preview | no | `preview-compiler.test.js`, `preview-compile-client.test.js`, `golden-page-state.test.mjs` | `preview-ready-not-stuck-gate.mjs`, `project-runtime-failure-browser-gate.mjs`, `preview-shell-must-start-gate.mjs` | `golden:calculator`, `golden:iterate-heading`, `deployed-shop-preview-gate.mjs` | proven |
| `preview-repair` | A broken Preview repairs itself, or says it could not | yes | `refinement-loop.test.js`, `build-repair.test.js`, `verify-build.test.ts` | `qir-coding-resume-browser-gate.mjs`, `project-runtime-failure-browser-gate.mjs` | — | proven |
| `preview-controls` | Switch the Preview device, download the build, or ask to Improve it | yes | — | — | — | nothing |
| `desk-review-probes` | Review the build against probed criteria and patch what fails | yes | `outcome-gap-detection.test.js`, `desk-chat-claim-filter.test.js` | `desk-job-review-browser-gate.mjs`, `desk-review-patch-browser-gate.mjs`, `desk-chat-claim-browser-gate.mjs` | — | proven |
| `shop-build-cart` | A shop build shows its photos and prices, and the bag fills | no | `shop-ui-react-vfs.test.js`, `shop-catalog-photos-scale-gate.mjs` | `shop-preview-act-gate.mjs`, `desk-review-patch-browser-gate.mjs`, `studio-regression-browser-gate.mjs` | `deployed-shop-preview-gate.mjs` | proven |
| `desk-git` | Desk git: status, diff, commit, and review by hunk | no | `studio-git.test.js` | `desk-diff-review-browser-gate.mjs`, `studio-regression-browser-gate.mjs` | — | proven |
| `desk-chrome` | Desk chrome: rails, terminal, file tree, editor, splits and the Publish menu | no | — | `desk-rail-smoke-browser-gate.mjs`, `coding-desk-chrome-browser-gate.mjs`, `studio-regression-browser-gate.mjs` | — | proven |
| `desk-rewind` | Rewind the desk to an earlier checkpoint | no | `desk-checkpoints.test.js`, `desk-checkpoint-delta.test.js`, `desk-checkpoint-client.test.js` | — | — | helpers only |
| `qir-durable-run` | A coding run outlives the browser and the worker, and resumes | no | `qir-run-store.test.ts`, `qir-coding-runtime.test.ts`, `qir-coding-run-core.test.js`, `qir-durability.test.js`, `qir-production-recovery-contract.test.js`, `qir-persist-diagnosis.test.js` | `qir-coding-resume-browser-gate.mjs`, `qir-production-recovery-browser-gate.mjs` | `deployed-qir-durability-gate.mjs` | proven |
| `share-preview-link` | Copy a shareable link to the preview | no | `route-reachability.test.js` | `desk-publish-browser-gate.mjs` | — | proven |
| `publish-vercel` | Publish the build to Vercel | mixed | `route-reachability.test.js` | `desk-publish-browser-gate.mjs` | — | proven |
| `connect-domain` | Connect a custom domain | mixed | — | — | — | nothing |
| `deploy-gcp` | Deploy a full-stack app to Cloud Run | no | `route-reachability.test.js` | — | — | helpers only |
| `github-connect-destination` | Connect GitHub and choose the repository and branch a build writes to | no | `github-destination.test.js`, `github-workspace.test.js`, `github-principal.test.ts`, `github-write-seam-gate.mjs` | — | — | helpers only |
| `github-import-repo` | Import an existing repository into the desk | no | `github-import.test.js`, `github-checkout.test.ts`, `repository-preview.test.ts`, `github-intelligence.test.ts` | — | — | helpers only |
| `github-push` | Push the desk files to a new or existing repository | no | `github-push-payload.test.js`, `github-write-authorization.test.ts`, `github-write-seam-gate.mjs` | `desk-publish-browser-gate.mjs` | — | proven |
| `github-pull-requests` | Open, review, comment on and merge pull requests from the desk | no | `github-write-authorization.test.ts`, `github-tool-promise.test.ts`, `github-write-seam-gate.mjs` | — | — | helpers only |
| `office-artifacts` | Generate and download a PowerPoint, Excel, Word or PDF file | yes | `office-artifact.test.js`, `office-generation-budget.test.js`, `office-output-schemas.test.js`, `office-briefing.test.js`, `office-export.test.js`, `office-failure-copy.test.js`, `office-intent.test.js`, `office-words.test.mjs` | — | `golden:office-document` | proven |

### advisor desks

| journey | what a user does | model turn | deterministic | browser | deployed | status |
|---|---|---|---|---|---|---|
| `travel-desk` | Plan a trip: flights, stays, places and an itinerary | mixed | `travel-comprehension.test.js`, `travel-search-request.test.ts`, `travel-dates.test.ts`, `provider-refusal.test.js`, `travel-hotel-location.test.js` | `travel-browser-release-gate.mjs` | — | proven |
| `research-desk` | Research: a brief, claims verified, a topic watched, a deep dive | yes | `research-verify.test.ts`, `research-watch.test.ts`, `research-deep-dive.test.ts`, `research-claim-verifier.test.ts`, `research-board-actions.test.js`, `research-brief.test.js`, `grounding-marker.test.js` | — | — | helpers only |
| `finance-desk` | Finance: debt, savings, FX, market data and the advisor | mixed | `finance-judgment-gate.mjs`, `finance-advisor-gateway.test.ts`, `finance-gateway-guard.test.ts`, `debt-conversation.test.ts`, `financial-profile-gateway.test.ts`, `finance-board-brief.test.js` | `finance-browser-gate.mjs` | — | proven |
| `study-tutor` | Study: a tutor conversation with lessons, pictures and a syllabus | yes | `study-teaching-policy.test.ts`, `study-adaptive-lesson-loop.test.ts`, `study-conversation-loop.test.js`, `study-teaching-turn.test.js`, `study-representation-gate-contract.test.js`, `study-pictures.test.js` | `study-media-browser-gate.mjs`, `study-electricity-browser-gate.mjs`, `study-representation-browser-gate.mjs` | — | proven |
| `study-verified-check` | Study: take a verified check and get a verdict | no | `study-evidence.test.ts`, `study-assessment.test.ts`, `study-verification.test.ts`, `study-evidence-client.test.js` | `study-media-browser-gate.mjs`, `study-onboarding-reinforcement-browser-gate.mjs` | — | proven |
| `study-notebook-flashcards` | Study: notebook, flashcards and the assessment history | no | `study-notebook.test.ts`, `study-assessment-history.test.ts`, `study-notebook-client.test.js`, `study-assessment-history-client.test.js` | `study-notebook-browser-gate.mjs`, `study-media-browser-gate.mjs` | — | proven |
| `study-onboarding` | Study: onboarding and the reinforcement board | no | `study-onboarding.test.ts`, `study-reinforcement.test.js` | `study-onboarding-reinforcement-browser-gate.mjs` | — | proven |
| `verified-media-links` | A video offered in a reply is validated before it is offered | no | `youtube-media.test.js` | `media-profile-browser-gate.mjs`, `study-media-browser-gate.mjs` | — | proven |

### other surfaces

| journey | what a user does | model turn | deterministic | browser | deployed | status |
|---|---|---|---|---|---|---|
| `dream-to-action` | Dream-to-Action canvas: an idea becomes a thought, then an action | yes | — | — | — | parked |
| `quantum-playground` | Quantum Playground: build a circuit and watch the state | no | `statevector.test.js` | — | — | parked |
| `privacy-vault-byok` | Bring your own provider keys for this session | yes | `byok-credentials.test.ts` | — | — | helpers only |
| `feedback-widget` | Send product feedback | no | `feedback-store.test.ts`, `feedback-compose.test.js` | — | — | helpers only |
| `admin-dashboard` | Admin: metrics, analytics, feedback triage and the model dashboard | no | `model-dashboard-ranking.test.js`, `model-lifecycle.test.js`, `model-store.test.js` | — | — | helpers only |
| `isolated-desk-route` | Open the desk at its own address after signing in | no | `studio-isolation.test.js` | — | — | helpers only |
| `desktop-app` | Desktop app: open a folder, edit, run a shell, use git, chat, sign out | no | `git.test.ts`, `shell.test.ts`, `workspace-policy.test.ts`, `api-proxy-policy.test.ts` | `desktop-smoke-gate.mjs` | — | proven |
| `turn-reference-id` | A turn leaves a reference id, and a failed turn's reference resolves to what happened | no | `transaction-trace.test.js`, `transaction-trace.test.ts`, `listening-layer.test.js`, `trace-story.test.js`, `trace-lookup.test.js` | `second-transaction-browser-gate.mjs`, `trace-lookup-browser-gate.mjs` | `golden:calculator` | proven |

## Platform invariants

Not journeys, and not counted above, but every journey stands on them.

| invariant | what must hold | deterministic | browser | deployed |
|---|---|---|---|---|
| `functions-boot` | Every deployed function boots before its handler runs, and every import resolves in production | `runtime-import-gate.mjs` | — | `deployed-readiness-gate.mjs` |
| `routes-served-and-called` | Every route the desk calls is served, and every served route is called | `platform-dead-control-gate.mjs`, `served-route-gate.mjs`, `route-reachability.test.js` | — | — |
| `endpoints-protected` | Every model-facing endpoint refuses a blocked or missing session | `protected-endpoints.test.ts`, `authz-golden-canary.test.ts` | — | — |
| `claims-backed` | No capability chip or tool description promises what nothing answers | `capability-claims-gate.mjs` | — | — |
| `dead-wires` | Nothing tested is reachable by nothing, and hooks are declared in dependency order | `wiring-gate.mjs`, `hook-dependency-order-gate.mjs` | — | — |
| `hostile-reply-safety` | A hostile or malformed model reply cannot destroy the build | `pipeline-stress.mjs`, `never-discard-model-output.test.js`, `chat-turn-safety.test.js` | — | — |
| `experience-budget` | The shell, a workspace switch and the input stay inside their time budgets | — | `platform-experience-browser-gate.mjs` | — |
| `models-governed` | Models are listed, canaried, routed and retired on evidence | `model-canary.test.js`, `model-smoke-test.test.js`, `model-execution-policy.test.ts`, `server-key-resolution.test.ts`, `user-paid-quota.test.ts`, `user-paid-quota-wiring.test.ts`, `model-outcome-routing.test.js`, `latency-tie-breaker.test.js`, `provider-health.test.mjs` | — | `provider-health-probe.mjs` |
| `gates-anchored` | The gates anchor on hooks the app publishes, read the page state the desk writes, and pin the browser they run | `deployed-gate-contract.test.js`, `golden-page-state.test.mjs`, `golden-engine-refusal.test.mjs`, `golden-plan.test.mjs`, `zip-entry.test.mjs`, `business-tool-reconcile.test.mjs`, `workflow-playwright-pin.test.mjs`, `test-failure-summary.test.mjs` | — | — |
| `inventory-true` | This ledger names only gates that exist and run, claims every gate on disk, and its number is true | `journey-gate-inventory-gate.mjs`, `journey-gate-inventory.test.js` | — | — |

